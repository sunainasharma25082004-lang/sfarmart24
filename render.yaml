services:
  - type: web
    name: sfarmart24
    env: static
    rootDir: website
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
